class Singers {  

//give ability to concieve of x and y variables (to be given cordniants)
  constructor(x, y, voiceNumber, pitchNumber) {
    this.voiceNumber = voiceNumber
    this.pitchNumber = pitchNumber
    this.x = x
    this.y = y
  }
  
  update(note) {
    // note.voice == this.voiceNumber
  }
}

